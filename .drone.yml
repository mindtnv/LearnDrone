kind: pipeline
type: docker
name: default

steps:
  - name: build
    image: mcr.microsoft.com/dotnet/sdk:7.0
    commands:
      - dotnet build

  - name: docker-build
    image: plugins/docker
    settings:
      username: admin
      password: Warnet77
      registry: registry.dev.gbms.site
      dockerfile: LearnDrone/Dockerfile